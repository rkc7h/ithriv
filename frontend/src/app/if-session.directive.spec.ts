import { IfSessionDirective } from './if-session.directive';

describe('IfSessionDirective', () => {
  it('should create an instance', () => {
    const directive = new IfSessionDirective();
    expect(directive).toBeTruthy();
  });
});
