import { GradientBorderDirective } from './gradient-border.directive';

describe('GradientBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new GradientBorderDirective();
    expect(directive).toBeTruthy();
  });
});
